function App() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
      <div style={{ fontSize: 30, fontWeight: 600 }}>
        GitHub Action으로 CI/CD 구축해보기
      </div>
    </div>
  );
}

export default App;
